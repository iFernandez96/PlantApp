{
  "name": "@plantapp/backend",
  "version": "0.0.0",
  "private": true,
  "description": "PlantApp backend — Node.js + TypeScript. Slice 1 scaffolding only; no production behavior yet.",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "lint": "eslint .",
    "test": "vitest run",
    "test:int": "vitest run --config vitest.integration.config.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/src/server.js",
    "typecheck": "tsc --noEmit",
    "validate-schemas": "ajv compile --spec=draft2020 -c ajv-formats --all-errors --strict=true -s '../shared-schemas/*.schema.json'"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.106.2",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "fastify": "^5.8.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/pg": "^8.20.0",
    "ajv-cli": "^5.0.0",
    "eslint": "^9.17.0",
    "eslint-config-prettier": "^9.1.0",
    "pg": "^8.21.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.1",
    "vitest": "^2.1.8"
  }
}
