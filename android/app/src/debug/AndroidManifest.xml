<?xml version="1.0" encoding="utf-8"?>
<!-- Debug overlay: merges over the main manifest for debug builds only, pointing the app at the
     debug network-security-config (cleartext to a dev/LAN backend). Release builds are unaffected. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:networkSecurityConfig="@xml/network_security_config" />
</manifest>
